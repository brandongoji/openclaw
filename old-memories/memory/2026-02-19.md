# 2026-02-19

- User requested short responses due to chronic fatigue; prefer concise replies by default.
- Set up multi-agent routing for Discord:
  - `main` agent for private Hagios guild (`1473419068743618764`)
  - `pals` agent for Hagios Pals guild (`1473450991109144598`)
- Hardened Hagios Pals safety:
  - `pals` agent sandbox enabled (`mode: all`, `scope: session`, `workspaceAccess: none`)
  - `pals` tools restricted to chat-focused use; added explicit guild-level deny rules for risky tools.
- Per-server mention behavior set:
  - Private Hagios guild mention requirement OFF
  - Hagios Pals mention requirement ON
- Enabled `web_search` for Pals agent but live search still fails without supported provider key for current `web_search` path (Brave/Perplexity/Grok supported; Google CSE not native in this tool path).
- User identified prompt-injection risk in relaying Pals chat into main memory; decision: do NOT auto-forward raw Pals messages to main.
- Created reminder job for later Google-search setup work:
  - Job ID `5c5726c4-e260-486c-b87b-118d60474164`
  - Reminder date: 2026-03-19 12:00 PM local.
- User preference reinforced: UI-visible actions preferred; avoid hidden backend behavior for channel/setup changes when possible.
- User asked assistant to open and learn their local React app under `C:\Vibe Coding DO NOT DELETE`; confirmed app not reachable in browser until dev server is started in VS Code terminal, then share exact localhost URL.
- Gateway Dashboard top-nav steppers implemented and validated in Playwright:
  - Arrows now pan tabs only (no tab auto-switch/open).
  - Fixed glyph rendering to plain < and >.
  - Refactored responsive nav so arrows stay locked at left/right edges while tabs scroll in a separate .nav-scroll lane.
  - Added side padding in scroll lane (10px 92px) so tabs do not overlap button hit areas.
  - Verified with Playwright clicks and scroll metrics (scrollLeft advanced and returned as expected).
- User preference captured in USER.md: prefers agentic execution (assistant should do the work directly, then report results).

## 23:08 - Hagios Flow assistant settings simplification + encoding fixes
- Fixed mojibake/icon corruption in src/components/AssistantBubble.jsx (settings gear, minimize dash, chat bubble, thinking text, separators/remove glyphs) so icons/text render cleanly.
- Removed separate per-key model input from assistant settings UI to keep configuration minimal.
- Switched model resolution to parse from key label (format: Name - model-id), with fallback behavior in code.
- Updated defaults so new keys include model in label (e.g., Primary - gpt-4o-mini, Key N - gpt-4o-mini).
- Simplified settings UX per user request:
  - Added Add key button inline with the Active API key dropdown.
  - Removed standalone New key label entry field.
  - Kept only required runtime fields: active key selector, key label, API key, endpoint.
- Playwright-validated UI after edits on /notes; assistant panel shows updated control layout and corrected icons.
- Known operational note: model errors like The requested model 'Codex' does not exist now indicate the key label’s model segment is invalid for the selected endpoint and should be renamed to a valid model id.

## 23:13 - UI quality expectation from Brandon
- Brandon requested that I proactively catch UI visual consistency issues (theme mismatches, control styling/details) during implementation, without waiting to be told.
- Standard to apply going forward: self-check UI for visual parity and polish before handing off.
