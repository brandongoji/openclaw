
## Transcription Product Ideas (captured 2026-02-16 12:56 EST)
- Idea: Build an MVP app clicker/workflow where the phone connects over internet to the user's PC.
- Architecture thought:
  - OpenClaw runs on the user's PC.
  - PC acts as local transcription server (privacy/local-first processing).
  - Phone acts as remote capture/control client.
  - Audio from phone is sent to PC for transcription, then transcript returned to phone/app.
- Validation tasks:
  - Test end-to-end phone->PC remote transcription latency and reliability.
  - Test whether transcription can run directly on phone hardware as an alternative.
  - Compare phone-local transcription vs PC-hosted transcription quality/speed/cost.
- Goal:
  - Keep local-hosted option with OpenClaw PC server.
  - Offer best practical mode per user device capability.

## Next queued priorities (captured 2026-02-16 13:05 EST)
1. Finish better voice-to-text path (Whisper first if needed, Parakeet next).
2. Set up Discord server/channel workflow for assistant communication.
3. Teach multi-agent / multiple concurrent chat sessions in OpenClaw.

## Durable Preferences + Decisions (captured 2026-02-16 13:17 EST)
- User wants Git checkpoints throughout work, with short readable names including date/time + update summary in commit subject format:
  - ckpt | YYYY-MM-DD HH:MM EST | <what changed>
- User prefers autonomous mode with milestone updates and visible progress, not silent background work.
- User requested visible monitoring surfaces:
  - AUTONOMOUS-STATUS.md
  - AUTONOMOUS-LOG.txt
  - AUTONOMOUS-THINKING.md
  - single-window dashboard script + observer web panel.
- Voice-to-text quality is top priority; Moonshine accuracy considered poor by user.
- Execution strategy approved by user:
  - Retry Parakeet; if blocked, proceed to Whisper to ensure better communication quality quickly.
  - Use Codex for debugging/coding and Gemini for research/web-heavy tasks to preserve limits.
- Future queued priorities after STT stabilizes:
  1) Discord communication setup
  2) multi-agent / multiple concurrent OpenClaw chats

## Durable Build Progress (captured 2026-02-16 13:23 EST)
- User explicitly requested: start Whisper immediately and continue until done.
- Whisper MVP integration shipped in core repo:
  - Added gateway method file: src/gateway/server-methods/whisper.ts with whisper.transcribe calling OpenAI audio transcription API via OPENAI_API_KEY.
  - Registered method in gateway handler list and methods list.
  - Updated UI model selector to include whisper-1, and set default STT model to whisper-1 (best).
- Checkpoint pushed:
  - 31ddf446 — ckpt | 2026-02-16 13:22 EST | whisper transcribe path + model selector
- Operational blocker noted:
  - This shell lacked openclaw CLI path and gateway tool restart is disabled; manual gateway restart may be required on user side if hot reload is not active.

## Parakeet local STT setup + integration pass (captured 2026-02-16 16:50 EST)
- Install completed in workspace skill venv:
  - Command: `powershell -ExecutionPolicy Bypass -File skills/parakeet-local-stt/scripts/install-parakeet.ps1`
  - Result: `.venv` created at `skills/parakeet-local-stt/scripts/.venv` with `torch`, `torchaudio`, `nemo_toolkit[asr]`, etc.
- Smoke test against `moonshine_test.wav` (v2 model):
  - Command: `powershell -ExecutionPolicy Bypass -File skills/parakeet-local-stt/scripts/parakeet-transcribe.ps1 -InputFile C:\Users\brand\.openclaw\workspace\moonshine_test.wav -Model nvidia/parakeet-tdt-0.6b-v2 -OutputFile C:\Users\brand\.openclaw\workspace\checkpoints\parakeet-smoke-v2.txt`
  - Wall-time: `ELAPSED_SECONDS=88.45` (CPU path; CUDA unavailable warning shown)
  - Output file written successfully, but transcript content was empty for this sample (quality note: usable pipeline path, but this specific clip produced blank text).
- Reliability fix applied to local script:
  - Edited `skills/parakeet-local-stt/scripts/parakeet_transcribe.py` to avoid NeMo path-based transcribe crash (`TypeError` from lhotse sampler) by loading audio via `librosa` and using direct-audio transcribe with fallback.
- OpenClaw integration in repo (`C:\Vibe Coding DO NOT DELETE\OpenClaw\openclaw`):
  - Added new gateway method handler: `src/gateway/server-methods/parakeet.ts` (`parakeet.transcribe`)
  - Registered method in:
    - `src/gateway/server-methods.ts` (imports, auth write scope, handler map)
    - `src/gateway/server-methods-list.ts`
  - UI model dropdown + state updated to allow Parakeet selection:
    - `ui/src/ui/views/chat.ts` (new option `parakeet-v2`)
    - `ui/src/ui/app.ts` (routes `parakeet-v2` to `parakeet.transcribe` with model `nvidia/parakeet-tdt-0.6b-v2`)
    - `ui/src/ui/app-view-state.ts` (type union update)
- Build/typecheck attempt:
  - Command: `corepack pnpm -s exec tsc -p tsconfig.json --noEmit`
  - Result: failed on pre-existing unrelated TS error in `src/slack/monitor/slash.test-harness.ts` (TS2742), not caused by Parakeet changes.
- Next manual steps:
  1. Ensure gateway runtime can access workspace Parakeet skill path or copy/sync `skills/parakeet-local-stt` into repo root skill folder used by gateway.
  2. Re-run smoke test on a known speech-heavy WAV (current test file may be silent/too quiet for this model path).
  3. Re-run full project checks once existing TS2742 baseline issue is resolved.

## Operational safety preference (captured 2026-02-16 19:13 EST)
- User directive: do **not** perform disruptive gateway/process restarts during active work flow without explicit approval.
- For restart-required actions:
  - Ask user to run the restart in VS Code/terminal and wait.
  - If user is unavailable, defer/skip that step and continue non-disruptive work only.
- Preferred future flow: once Discord is configured, send a message to request manual restart/approval there.
