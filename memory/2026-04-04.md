- Diagnosed TellMeMo Android emulator mic issue: emulator originally launched without `-allow-host-audio`, causing zeroed host audio. Relaunched `tellmemo-avd` with host audio enabled and confirmed TellMeMo had Android mic permission and could start/stop/upload recordings, but transcriptions remained empty / reported 0-second audio.
- Isolated likely remaining blocker to macOS TCC microphone permission for Android Emulator / QEMU process chain; user screenshot of System Settings → Privacy & Security → Microphone showed no entries for emulator at that time.
- Created durable emulator launcher `~/bin/run-tellmemo-avd.sh` to start `tellmemo-avd` with `-allow-host-audio` and handle stale AVD lock cleanup; documented workflow in `notes/TellMeMo-Hagios-Transcriber.md`.
- Later diagnosis shifted to network/login issue in emulator: host internet and Cloudflare were up, emulator connectivity was briefly flaky but recovered (`PROBE_HTTP/HTTPS 204`), while TellMeMo app specifically failed connecting to `https://api.tellmemo.io/api/v1/auth/login` with repeated connection retries. Likely issue is TellMeMo API path/emulator network path rather than a general Cloudflare outage.
- Diagnosed Hagios 1 main-session drift/freeze root causes in live `state/openclaw.json`: (1) default main-agent model had been switched to `ollama/gemma4:e2b`, with repeated assistant timeouts in logs; (2) the public Discord `pals` account was incorrectly bound to agent `main`; (3) Discord exec approval filters still included public-channel session ids; (4) `workspace/IDENTITY.md` was still blank, weakening continuity after resets. Patched the config so main defaults back to `openai-codex/gpt-5.4`, `pals` routes to agent `pals`, public exec approvals are disabled/restricted, and identity continuity files now explicitly define Hagios/Hagios Pal separation.
