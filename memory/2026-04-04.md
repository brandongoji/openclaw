# 2026-04-04

- Restored and stabilized Hagios 1 model selection after repeated picker drift.
- Guardrail: keep the non-Ollama default model as primary; local Ollama models may remain installed and manually selectable, but must not be auto-removed, hidden, or promoted to main.
- Keep the model allowlist/provider catalog in sync instead of hand-editing picker entries.
