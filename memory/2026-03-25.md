# 2026-03-25

- User preference: keep responses short and direct when asked; avoid long explanations unless requested.
- User requested approval-flow behavior change: send one approval request, then wait (no rapid re-requests/retries) unless user says "resend" or timeout is explicitly reported.
- Git checkpoint preference: commit message format should include date/time and short description (`MM/DD/YYYY HH:MM AM/PM - <very short update description>`).
- Discord/OpenClaw setup intent:
  - `#hagios` is the private control channel.
  - Hagios Pals server should be chat-capable but non-PC-control-capable via restricted policy.
  - New separate bot/app `Hagios Pal` was created and intended for Hagios Pals isolation.
- New skills/work created in workspace:
  - `api-limit-alert-by-hagios` (weekly + 5h usage alerts with 10% steps, 25% warning, 10% red alert).
  - `response-delivery-monitor-by-hagios` (detect likely missed assistant replies; heartbeat-friendly checker).
  - `playwright-hagios` (inspect/snapshot-first navigation protocol).
- User concern to preserve privacy before GitHub push: treat `state/`, `memory/` JSON/state, secrets/tokens, and logs as private/non-public by default.
